<div class="col-md-6 mx-auto text-center">
            <h1 class="wv-heading--title">
               Log In
            </h1>
            <h2 class="wv-heading--subtitle">
              Welcome back! Let's have a good time again!
            </h2>
      </div>
      <br>
<div class="row">
<div class="col-md-4 mx-auto">
    <div class="myform form ">
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field form-group ">
    <%= f.text_field :login, placeholder: "Username or Email",:class=>"form-control my-input" %>
  </div>

  <div class="field form-group">
    <%= f.password_field :password, placeholder: :password, autocomplete: "current-password" ,:class=>"form-control my-input" %>
  </div>

  <% if devise_mapping.rememberable? %>

    <div class="field form-group form-check">
      <%= f.check_box :remember_me, :class=>"form-check-input" ,:id=>"materialUnchecked"%>
      <%= f.label :remember_me,:class=>"form-check-label", :for=>"materialUnchecked" %>
    </div>
  <% end %>

  <div class="actions text-center">
    <%= f.submit "Log in", :class=>"btn btn-block g-button"%>
  </div>
<% end %>
<div class="col-md-12 ">
                     <div class="login-or">
                        <hr class="hr-or">
                        <span class="span-or">or</span>
                     </div>
                  </div>
<div class="form-group">

<%= render "devise/shared/links" %>
</div>
</div>
</div>
</div>

